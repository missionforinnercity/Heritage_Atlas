{
  "name": "app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "data:build": "node scripts/build-geojson.mjs",
    "dev": "npm run data:build && vite",
    "build": "npm run data:build && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  },
  "dependencies": {
    "mapbox-gl": "^3.18.1",
    "xlsx": "^0.18.5"
  }
}
